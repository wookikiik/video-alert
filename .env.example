# Frontend Environment Variables
# Copy this file to frontend/.env.local and update values as needed

# Backend API Base URL
# Used by the Next.js frontend to connect to the FastAPI backend
# In local development, this should point to your local backend server
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# ============================================================================
# INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.example frontend/.env.local
# 2. Update NEXT_PUBLIC_API_BASE_URL if your backend runs on a different port
# 3. NEVER commit .env.local to version control (it's in .gitignore)
# 4. After editing .env.local, restart the Next.js dev server to pick up changes
# ============================================================================

# ============================================================================
# NOTES
# ============================================================================
# - NEXT_PUBLIC_* variables are exposed to the browser (client-side)
# - Environment variables without NEXT_PUBLIC_ prefix are only available server-side
# - The backend API handles authentication and serves admin endpoints like:
#   - GET /api/v1/admin/system-variables (read-only system configuration)
# - For backend setup and authentication details, see docs/DEV_SETUP_FRONTEND.md
# ============================================================================

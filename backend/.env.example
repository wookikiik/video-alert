# Backend Environment Variables
# Copy this file to backend/.env and update values as needed
# WARNING: Never commit the .env file to version control!

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
PROJECT_NAME=Video Alert API
VERSION=1.0.0
API_V1_PREFIX=/api/v1

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# SQLite database URL for local development
# Format: sqlite:///./path/to/database.db
# The './' makes it relative to the backend directory
DATABASE_URL=sqlite:///./dev.db

# ============================================================================
# MONITORED URL
# ============================================================================
# The URL to monitor for video alerts
# Example: https://example.com/videos
MONITORED_URL=https://example.com/videos

# ============================================================================
# TELEGRAM BOT CONFIGURATION
# ============================================================================
# Telegram Bot Token - obtained from @BotFather
# WARNING: This is a SECRET value - NEVER commit it to git!
# Example format: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Telegram Channel ID where alerts will be sent
# Can be a channel username (e.g., @mychannel) or numeric ID (e.g., -1001234567890)
# To get your channel ID:
# 1. Add your bot to the channel as an administrator
# 2. Send a message to the channel
# 3. Visit https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# 4. Look for the "chat" object and note the "id" field
TELEGRAM_CHANNEL_ID=@your_channel_id

# ============================================================================
# SCHEDULER CONFIGURATION
# ============================================================================
# Enable/disable the scheduler (true/false)
SCHEDULER_ENABLED=true

# How often to check for new videos (in seconds)
# Default: 300 (5 minutes)
SCHEDULER_INTERVAL=300

# ============================================================================
# CORS SETTINGS
# ============================================================================
# Comma-separated list of allowed origins for CORS
# For local development, include your frontend URL
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================
# Set to 'development' for local dev, 'production' for production
ENVIRONMENT=development

# Enable debug mode (true/false)
# WARNING: Never enable in production!
DEBUG=true

# ============================================================================
# INSTRUCTIONS
# ============================================================================
# 1. Copy this file to create your local environment file:
#    cp backend/.env.example backend/.env
#
# 2. Edit backend/.env and fill in real values:
#    - Get TELEGRAM_BOT_TOKEN from Telegram's @BotFather
#    - Set your TELEGRAM_CHANNEL_ID (channel or group where bot will send messages)
#    - Set MONITORED_URL to the actual URL you want to monitor
#    - Adjust SCHEDULER_INTERVAL as needed
#
# 3. NEVER commit backend/.env to version control!
#    It is already in .gitignore
#
# 4. Keep your bot token secure:
#    - Don't share it publicly
#    - Don't include it in screenshots or logs
#    - Rotate it immediately if compromised (via @BotFather)
# ============================================================================
